/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/ESAUCSDWEBSITE',
  images: {
    unoptimized: true,
  },
  trailingSlash: true,
}

module.exports = nextConfig 